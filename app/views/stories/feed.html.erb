<div class="page-header">
	<%# link_to('Subscribe', new_subscription_path, class: 'btn btn-primary pull-right') %>
	<h1>Feed</h1>
</div>

<div class="row" id="stories">
	<%= render(@stories, context: :feed) %>
</div>
