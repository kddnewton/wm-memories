<div class="page-header">
  <%= link_to(vt('subscribe'), new_subscription_path, class: 'btn btn-primary pull-right') %>
  <h1><%= vt('feed') %></h1>
</div>

<div class="row" id="stories">
  <%= render(collection: @stories, partial: 'stories/story', query: @query) %>
</div>
