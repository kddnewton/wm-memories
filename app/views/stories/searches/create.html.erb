<div class="page-header">
  <h1><%= vt('search') %> <small><%= @query %></small></h1>
</div>

<div class="row" id="stories">
  <%= render(collection: @stories, partial: 'stories/story', query: @query) %>
</div>
