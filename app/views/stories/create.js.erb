$("#new-story").modal("hide");

var map = window.Memories.activeMarker.map;
var position = window.Memories.activeMarker.position;

window.Memories.activeMarker.setMap(null);
window.Memories.loadMarker(map, position.A, position.F, "<%= j truncate(@story.body, length: 200) %>", <%= @story.id %>);
